/**
 * 機能のオンオフをまとめて管理する。
 *
 * AI機能(Claude API)は使うたびに料金がかかるうえ、クレジットが無いと
 * ボタンを押してもエラーになる。押せるのに動かないボタンを見せない方が
 * 分かりやすいので、既定では隠しておく。
 *
 * 使いたくなったら AI_ENABLED を true に戻すだけで、
 * レシピ提案・献立提案・レシート読み取りがまとめて復活する。
 */
export const AI_ENABLED = false;
